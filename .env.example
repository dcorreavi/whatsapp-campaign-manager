# Database
DATABASE_URL=postgresql://username:password@host:port/database

# Server
NODE_ENV=development
PORT=5000

# PostgreSQL connection details (automatically set by some providers)
PGHOST=
PGPORT=5432
PGUSER=
PGPASSWORD=
PGDATABASE=